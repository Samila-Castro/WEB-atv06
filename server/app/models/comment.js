module.exports = (sequelize, DataTypes) => {
    const Comment = sequelize.define('Comment', {
    //   id: DataTypes.INTEGER,  
      // id: DataTypes.STRING,
     
      
      
    });
  
    return Comment;
  }